<h1>New Game</h1>

<%= form_with model: @game do |f| %>
    <%# f.label :minimum_bet %>
    <%# f.number_field :min_bet %>
    <%# f.label :maximum_bet %>
    <%# f.number_field :max_bet %>
    <%# f.label :player %>
    <%# f.collection_select(:player_id, @players, :id, :name) %>
    <%= f.label :bet %>
    <%= f.select(:bet, [10, 15, 20, 25, 30, 35, 40, 45, 50]) %>
    <br>
    <br>
    <%= f.label :dealer %>
    <%= f.collection_select(:dealer_id, @dealers, :id, :name) %>
    <br>
    <br>
    <%= f.submit "Start Game" %>
<% end %>

<ul>
    <% if flash[:error] %>
        <li class= "errors"><%= flash[:error] %></li>
    <% end %>
</ul>